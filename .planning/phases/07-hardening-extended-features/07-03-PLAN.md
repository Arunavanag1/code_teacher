---
phase: 07-hardening-extended-features
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - package.json
  - README.md
autonomous: true
requirements: [REQ-17, REQ-20]
user_setup: []

must_haves:
  truths:
    - "package.json version is updated to 1.0.0"
    - "package.json includes an 'engines' field specifying 'node' >= '22.0.0'"
    - "package.json includes a 'prepublishOnly' script that runs 'npm run build'"
    - "package.json 'files' array includes 'dist/', 'agents/definitions/', and 'README.md'"
    - "package.json 'keywords' array is comprehensive and includes relevant search terms"
    - "README.md exists at the project root with sections: overview/description, requirements, installation, quick start, CLI commands with examples, configuration file reference, LLM provider setup, custom agents with template, output modes with sample output, watch mode, architecture with pipeline diagram, error handling, license"
    - "README.md documents all CLI flags: --mode, --file, --verbose, --top, --json, --provider, --model, --watch"
    - "README.md documents the init command: code-teacher init [path] --force"
    - "README.md documents all three output modes with examples"
    - "README.md documents the code-teacher.config.json schema with all fields"
    - "README.md documents the auto-detection order for LLM providers"
    - "README.md documents custom agent support with a brief example"
    - "README.md includes a brief architecture section describing the agent system and two-stage pipeline"
    - "A dry-run verification confirms that 'npm run build' succeeds, the CLI --help produces output with all commands and flags, and 'code-teacher init' creates a config file"
    - "TypeScript compiles without errors via npx tsc --noEmit"
    - "ESLint passes via npx eslint ."
  artifacts:
    - path: "package.json"
      provides: "Production-ready package.json with version 1.0.0, engines, prepublishOnly, and comprehensive metadata"
      exports: []
      min_lines: 45
    - path: "README.md"
      provides: "Comprehensive documentation with installation, usage, configuration, provider setup, custom agents, output modes, and architecture"
      exports: []
      min_lines: 300
  key_links:
    - from: "README.md"
      to: "code-teacher_spec.md"
      via: "README documents the spec's CLI commands, flags, config schema, output formats, and provider detection exactly"
      pattern: "code-teacher analyze.*--mode.*--file.*--verbose"
    - from: "package.json"
      to: "README.md"
      via: "package.json files array includes README.md so it is distributed with the package"
      pattern: "files.*README.md"
---

<objective>
Prepare the project for publishing and validate it works end-to-end: update package.json to v1.0.0 with publishing metadata, create a comprehensive README.md, and run a dry-run verification confirming the build succeeds and CLI commands work.

Purpose: This plan makes code-teacher ready for distribution via `npm install -g github:USERNAME/code-teacher`. The README is the first thing users see and must clearly document installation, usage, and configuration. The package.json updates ensure the package installs and runs correctly when consumed.

Output: Updated package.json at v1.0.0 with publishing fields, and a new README.md at the project root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening-extended-features/07-RESEARCH.md
@.planning/phases/07-hardening-extended-features/07-01-SUMMARY.md
@.planning/phases/07-hardening-extended-features/07-02-SUMMARY.md
@code-teacher_spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json with publishing metadata</name>
  <files>package.json</files>
  <action>
    Update package.json with version 1.0.0, engines, prepublishOnly, and comprehensive metadata for publishing.

    1. **Update version** from "0.1.0" to "1.0.0".

    2. **Add engines field** (after the "type" field):
       ```json
       "engines": {
         "node": ">=22.0.0"
       },
       ```
       This reflects the Node.js 22+ requirement (implied by @types/node ^25.3.0 and fs.watch recursive usage).

    3. **Add prepublishOnly script** to the scripts block:
       ```json
       "prepublishOnly": "npm run build"
       ```
       This ensures TypeScript is compiled before publishing.

    4. **Update files array** to include README.md:
       ```json
       "files": [
         "dist/",
         "agents/definitions/",
         "README.md"
       ],
       ```

    5. **Update keywords** with more comprehensive search terms:
       ```json
       "keywords": [
         "code-analysis",
         "codebase",
         "teaching",
         "learning",
         "cli",
         "llm",
         "ai",
         "dependency-analysis",
         "code-review",
         "developer-tools"
       ],
       ```

    KEY NOTES:
    - engines.node >= 22.0.0 because fs.watch recursive is required (Node.js 19.1+) and the project uses @types/node ^25.3.0
    - prepublishOnly runs before `npm publish` — ensures dist/ is always up to date
    - files array ensures only dist/, agent definitions, and README are included in the published package (not source .ts files)
    - version 1.0.0 signals the first public release
  </action>
  <verify>
    - package.json version is "1.0.0"
    - package.json has engines.node >= "22.0.0"
    - package.json has prepublishOnly script
    - package.json files array includes README.md
    - `npm run build` succeeds
    - JSON is valid (no syntax errors in package.json)
  </verify>
  <done>package.json updated to v1.0.0 with engines, prepublishOnly, updated files array, and comprehensive keywords</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with comprehensive documentation</name>
  <files>README.md</files>
  <action>
    Create README.md at the project root with full documentation covering installation, usage, configuration, and architecture.

    Write the complete README.md with the following sections (content should be derived from code-teacher_spec.md and the actual implemented features):

    1. **Title and overview** — What code-teacher is, the three categories of insight (teachable sections, high-impact sections, data structure decisions), language-agnostic design

    2. **Installation** — `npm install -g github:USERNAME/code-teacher` and `npx github:USERNAME/code-teacher analyze ./my-project`

    3. **Quick Start** — Minimal steps: set an API key, run analyze

    4. **CLI Commands**:
       - `code-teacher analyze [path]` with ALL flags: --mode, --file, --verbose, --top, --json, --provider, --model, --watch
       - `code-teacher init [path]` with --force flag
       - Examples for each command and flag combination

    5. **Configuration** — `code-teacher.config.json` schema:
       - ignore: string[] (default ignore patterns)
       - maxFileSize: number (default 50000)
       - topN: number (default 5)
       - provider: string (optional)
       - model: string (optional)
       - customAgents: string[] (optional)

    6. **LLM Provider Setup**:
       - Auto-detection order: ANTHROPIC_API_KEY -> OPENAI_API_KEY -> GOOGLE_API_KEY
       - CODE_TEACHER_PROVIDER and CODE_TEACHER_MODEL env vars
       - --provider and --model CLI flags for explicit override
       - Detection priority flow

    7. **Output Modes**:
       - Summary (default) — description and example output
       - Verbose (--verbose) — adds agent reasoning traces
       - JSON (--json) — structured output for piping

    8. **Custom Agents** — How to write a custom agent .md file, the expected markdown format (Role, System Prompt, Input, Scoring Rubric, Output Schema sections), how to reference it in config

    9. **Watch Mode** — How --watch works, what triggers re-analysis

    10. **Architecture** — Brief overview of the agent system, the four built-in agents, the two-stage pipeline (parallel Stage 1, sequential Stage 2), the dependency graph

    11. **Error Handling** — Brief mention of exponential backoff on rate limits, clear error messages, graceful handling of unreadable files

    12. **License** — ISC (matching package.json)

    13. **Requirements** — Node.js >= 22, an API key for at least one supported provider

    KEY NOTES:
    - The README MUST be very comprehensive (300+ lines) and easily readable — this is the primary user-facing documentation
    - Use clear section headers with descriptive headings — readers should be able to scan and find what they need
    - Every CLI command and flag should have a concrete usage example with realistic arguments
    - Use tables for structured reference data (flags, config fields, environment variables)
    - Use code blocks for all CLI commands, config examples, and output samples
    - Include a sample terminal output showing what the summary mode looks like (use a simplified ASCII version without ANSI codes)
    - The Quick Start should be copy-pasteable — a new user should go from zero to first analysis in under 2 minutes
    - Configuration section should show a complete example config file with comments explaining each field
    - Custom agent section should include the full markdown template format with all 5 sections (Role, System Prompt, Input, Scoring Rubric, Output Schema)
    - Provider section should include a table of supported providers with their env vars, default models, and detection order
    - Output modes section should show example output for each mode (summary, verbose, JSON schema)
    - Architecture section should include a text-based diagram of the two-stage pipeline flow
    - Use bullet points and short paragraphs — avoid walls of text
    - Do not include screenshots or images (this is a CLI tool)
    - All examples should use realistic but simple command invocations
  </action>
  <verify>
    - README.md exists at the project root
    - README.md includes installation instructions
    - README.md documents all CLI flags: --mode, --file, --verbose, --top, --json, --provider, --model, --watch
    - README.md documents init command with --force
    - README.md documents code-teacher.config.json schema
    - README.md documents auto-detection order for providers
    - README.md documents all three output modes
    - README.md documents custom agent format
    - README.md documents watch mode
    - README.md is at least 300 lines
    - README.md includes tables for CLI flags, config fields, and provider details
    - README.md includes sample terminal output for summary mode
    - README.md includes a text-based pipeline diagram in the architecture section
    - README.md custom agents section includes the full 5-section markdown template
  </verify>
  <done>README.md created with comprehensive documentation: installation, CLI reference, configuration, providers, output modes, custom agents, watch mode, and architecture</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run verification of build and CLI</name>
  <files></files>
  <action>
    Run a dry-run verification to confirm the project builds and CLI commands work.

    1. **Run the build**:
       ```bash
       npm run build
       ```
       Verify: no TypeScript compilation errors.

    2. **Run linting**:
       ```bash
       npx eslint .
       npx prettier --check .
       ```
       Verify: no linting or formatting errors.

    3. **Test CLI --help**:
       ```bash
       node dist/cli/index.js --help
       ```
       Verify: output includes "analyze" and "init" commands.

    4. **Test analyze --help**:
       ```bash
       node dist/cli/index.js analyze --help
       ```
       Verify: output includes all flags: --mode, --file, --verbose, --top, --json, --provider, --model, --watch.

    5. **Test init command**:
       ```bash
       # Create a temp directory, run init, verify config created
       mkdir -p /tmp/code-teacher-test
       node dist/cli/index.js init /tmp/code-teacher-test
       cat /tmp/code-teacher-test/code-teacher.config.json
       # Verify it contains ignore, maxFileSize, topN, customAgents
       ```

    6. **Test init --force behavior**:
       ```bash
       # Run init again without --force — should print "already exists" message
       node dist/cli/index.js init /tmp/code-teacher-test
       # Run init with --force — should succeed
       node dist/cli/index.js init /tmp/code-teacher-test --force
       # Cleanup
       rm -rf /tmp/code-teacher-test
       ```

    7. **Test analyze with no API key** (verifies error handling):
       ```bash
       # Temporarily unset all API keys and run analyze
       # This should print a clean error message, not a stack trace
       env -u ANTHROPIC_API_KEY -u OPENAI_API_KEY -u GOOGLE_API_KEY node dist/cli/index.js analyze .
       ```
       Verify: output is a clean error message about missing API keys (no stack trace).

    8. **Test --version**:
       ```bash
       node dist/cli/index.js --version
       ```
       Verify: output is "1.0.0".

    KEY NOTES:
    - This task is purely verification — no code changes
    - All tests use the compiled dist/ output, not the TypeScript source
    - The "analyze with no API key" test verifies that Plan 07-01's error handling works end-to-end
    - The init tests verify Plan 07-02's init command works end-to-end
    - If any verification fails, go back and fix the issue before proceeding
  </action>
  <verify>
    - npm run build succeeds with zero errors
    - npx eslint . passes
    - npx prettier --check . passes
    - code-teacher --help shows analyze and init commands
    - code-teacher analyze --help shows all flags including --watch
    - code-teacher init creates a valid config file
    - code-teacher init without --force refuses to overwrite
    - code-teacher init --force overwrites successfully
    - code-teacher analyze with no API keys produces clean error message (no stack trace)
    - code-teacher --version outputs "1.0.0"
  </verify>
  <done>Dry-run verification complete: build, lint, CLI help, init command, error handling, and version all pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with zero errors
- [ ] `npx eslint .` passes
- [ ] `npx prettier --check .` passes
- [ ] package.json version is "1.0.0"
- [ ] package.json has engines.node >= "22.0.0"
- [ ] package.json has prepublishOnly script
- [ ] package.json files array includes README.md
- [ ] README.md exists with at least 300 lines
- [ ] README.md documents installation, usage, configuration, providers, output modes, custom agents, watch mode
- [ ] `code-teacher --help` shows analyze and init commands
- [ ] `code-teacher analyze --help` shows all flags including --watch
- [ ] `code-teacher init` creates a valid config file
- [ ] `code-teacher --version` outputs "1.0.0"
- [ ] Running analyze with no API keys produces a clean error message
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- package.json is production-ready with:
  - Version 1.0.0
  - Node.js engine requirement
  - prepublishOnly build script
  - Complete files array
- README.md is comprehensive with:
  - Installation and quick start
  - Complete CLI reference (all commands, all flags)
  - Configuration file schema
  - LLM provider setup guide
  - Output mode documentation
  - Custom agent guide
  - Architecture overview
- Dry-run verification confirms:
  - Build succeeds
  - All CLI commands work
  - Error handling produces clean messages
  - Version is correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-extended-features/07-03-SUMMARY.md`
</output>
