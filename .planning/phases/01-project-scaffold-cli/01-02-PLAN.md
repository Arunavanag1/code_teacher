---
phase: 01-project-scaffold-cli
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [cli/index.ts, cli/commands/analyze.ts, cli/commands/teachings.ts, cli/commands/sections.ts, package.json]
autonomous: true
requirements: [REQ-01]
user_setup: []

must_haves:
  truths:
    - "`code-teacher analyze [path]` accepts --mode, --file, --verbose, --top, --json, --provider, --model flags"
    - "`code-teacher analyze [path] --mode teachings` sets mode to teachings"
    - "`code-teacher analyze [path] --mode sections` sets mode to sections"
    - "`code-teacher analyze [path] --file src/foo.ts` targets specific file"
    - "`code-teacher analyze [path] --verbose` enables verbose output"
    - "`code-teacher analyze [path] --top 10` sets topN to 10"
    - "`code-teacher analyze [path] --json` enables JSON output"
    - "`code-teacher analyze [path] --provider anthropic --model claude-sonnet-4-20250514` overrides provider and model"
    - "`code-teacher --version` prints the version from package.json"
    - "`code-teacher --help` prints usage information"
    - "Config file is loaded from project path when present"
    - "CLI flag values override config file values"
  artifacts:
    - path: "cli/index.ts"
      provides: "CLI entry point with commander/yargs setup and all flags"
      min_lines: 40
      contains: "analyze"
    - path: "cli/commands/analyze.ts"
      provides: "Main analysis command handler that merges config + CLI flags"
      min_lines: 30
      exports: ["analyzeCommand"]
  key_links:
    - from: "cli/index.ts"
      to: "cli/commands/analyze.ts"
      via: "command registration"
      pattern: "import.*analyze"
    - from: "cli/commands/analyze.ts"
      to: "config/schema.ts"
      via: "config loading"
      pattern: "import.*loadConfig|validateConfig"
---

<objective>
Implement the full CLI framework with commander/yargs, all flags from the spec, and config file integration.

Purpose: Users interact with code-teacher entirely through this CLI — it must parse all flags correctly, load config, and provide helpful --help and --version output. This is the user-facing skeleton that all analysis features plug into.
Output: Working CLI binary that accepts all spec flags, loads config, and produces help/version output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@code-teacher_spec.md
@.planning/phases/01-project-scaffold-cli/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install CLI framework and implement entry point with all flags</name>
  <files>cli/index.ts, package.json</files>
  <action>
    1. Install commander: `npm install commander`
    2. Implement cli/index.ts:
       - Add shebang: #!/usr/bin/env node
       - Import { Command } from 'commander'
       - Import { version } from '../package.json' (with resolveJsonModule in tsconfig)
       - Import { loadConfig } from '../config/schema.js'
       - Create program with name('code-teacher'), description, version
       - Register 'analyze' command as the main command:
         - Argument: [path] (optional, defaults to '.')
         - Options:
           --mode <mode>       Analysis mode: 'teachings', 'sections', or 'all' (default: 'all')
           --file <path>       Analyze a specific file instead of full project
           --verbose           Show agent reasoning traces
           --top <n>           Number of top results to show (default: 5)
           --json              Output raw JSON instead of formatted terminal output
           --provider <name>   LLM provider: 'anthropic', 'openai', or 'google'
           --model <name>      Specific model to use
       - Parse and dispatch to analyzeCommand
       - Handle unknown commands with helpful error message
    3. Update tsconfig.json to add "resolveJsonModule": true if needed
    4. Verify: `npx tsc --noEmit` passes
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx tsc && node dist/cli/index.js --help` shows all flags
    - `npx tsc && node dist/cli/index.js --version` prints version
  </verify>
  <done>CLI entry point compiles and produces correct help/version output with all flags documented</done>
</task>

<task type="auto">
  <name>Task 2: Implement analyze command handler with config merging</name>
  <files>cli/commands/analyze.ts, cli/commands/teachings.ts, cli/commands/sections.ts</files>
  <action>
    1. Implement cli/commands/analyze.ts:
       - Export interface AnalyzeOptions: { mode, file, verbose, top, json, provider, model }
       - Export async function analyzeCommand(path: string, options: AnalyzeOptions)
       - Inside: load config via loadConfig(path), merge CLI options over config values (CLI wins)
       - Resolved config: { targetPath, mode, file, verbose, topN, json, provider, model, ignore, maxFileSize, customAgents }
       - For now, print the resolved config as confirmation (actual analysis comes in later phases)
       - Print startup line: "Using [provider] ([model]) — detected from [source]" (placeholder for now, just show config values)
    2. Implement cli/commands/teachings.ts stub:
       - Export function that delegates to analyzeCommand with mode='teachings'
    3. Implement cli/commands/sections.ts stub:
       - Export function that delegates to analyzeCommand with mode='sections'
    4. Verify the full flow: build and run with various flag combinations
  </action>
  <verify>
    - `npx tsc && node dist/cli/index.js analyze . --mode teachings --top 10 --verbose` shows resolved config with mode=teachings, top=10, verbose=true
    - `npx tsc && node dist/cli/index.js analyze . --json` shows json=true
    - `npx tsc && node dist/cli/index.js analyze . --provider openai --model gpt-4o` shows provider=openai, model=gpt-4o
    - `npx tsc && node dist/cli/index.js analyze /some/path --file src/foo.ts` shows targetPath and file
    - Running without flags uses defaults from config/defaults.ts
  </verify>
  <done>analyze command correctly parses all flags, loads and merges config, and displays resolved configuration</done>
</task>

<task type="auto">
  <name>Task 3: Build and verify end-to-end CLI binary</name>
  <files>package.json</files>
  <action>
    1. Run `npm run build` to compile TypeScript
    2. Test the built binary directly:
       - `node dist/cli/index.js --help` — all flags shown
       - `node dist/cli/index.js --version` — version from package.json
       - `node dist/cli/index.js analyze .` — runs with defaults
       - `node dist/cli/index.js analyze . --mode teachings --verbose --top 3` — flags parsed correctly
       - `node dist/cli/index.js analyze . --json --provider anthropic --model claude-sonnet-4-20250514` — all flags work
       - `node dist/cli/index.js analyze . --file cli/index.ts` — file targeting works
    3. Create a test code-teacher.config.json in project root:
       { "topN": 10, "ignore": ["test/**"], "maxFileSize": 100000 }
       Run `node dist/cli/index.js analyze .` — verify config loaded and merged
       Run `node dist/cli/index.js analyze . --top 3` — verify CLI flag overrides config topN
       Remove the test config file after verification
    4. Verify `npx eslint .` passes
    5. Verify `npx prettier --check .` passes (run `npx prettier --write .` first if needed)
  </action>
  <verify>
    - `npm run build` completes without errors
    - All CLI flag combinations produce expected output
    - Config file loading and merging works correctly
    - CLI flag overrides config file value
    - ESLint and Prettier pass
  </verify>
  <done>CLI binary fully functional — accepts all flags, loads config, merges correctly, and produces help/version output</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with zero errors
- [ ] `node dist/cli/index.js --help` lists all flags
- [ ] `node dist/cli/index.js --version` prints correct version
- [ ] `node dist/cli/index.js analyze . --mode teachings` works
- [ ] `node dist/cli/index.js analyze . --json --verbose --top 10` all flags parsed correctly
- [ ] `node dist/cli/index.js analyze . --provider openai --model gpt-4o` overrides work
- [ ] Config file loads when present and merges with CLI flags (CLI wins)
- [ ] `npx eslint .` passes
- [ ] `npx prettier --check .` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every flag from the spec is implemented and functional
- Config loading, validation, and merging with CLI flags works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-cli/01-02-SUMMARY.md`
</output>
