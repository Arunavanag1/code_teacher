---
phase: 01-project-scaffold-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, .eslintrc.json, .prettierrc, .gitignore, cli/index.ts, config/defaults.ts, config/schema.ts]
autonomous: true
requirements: [REQ-15]
user_setup: []

must_haves:
  truths:
    - "TypeScript compiles without errors via `npx tsc --noEmit`"
    - "ESLint runs without errors via `npx eslint .`"
    - "Prettier check passes via `npx prettier --check .`"
    - "package.json has correct bin field pointing to compiled CLI entry point"
    - "package.json supports `npm install -g github:USERNAME/code-teacher` distribution"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with dependencies, bin field, and GitHub distribution config"
      contains: "code-teacher"
    - path: "tsconfig.json"
      provides: "TypeScript configuration targeting Node.js"
      contains: "compilerOptions"
    - path: ".eslintrc.json"
      provides: "ESLint configuration for TypeScript"
    - path: ".prettierrc"
      provides: "Prettier formatting rules"
    - path: "cli/index.ts"
      provides: "CLI entry point stub"
    - path: "config/defaults.ts"
      provides: "Default configuration values"
      exports: ["defaults"]
    - path: "config/schema.ts"
      provides: "Config file validation schema"
      exports: ["validateConfig"]
  key_links:
    - from: "package.json"
      to: "cli/index.ts"
      via: "bin field"
      pattern: "\"bin\""
---

<objective>
Initialize TypeScript project with all tooling and the directory structure from the spec.

Purpose: Create the foundational project scaffold that all subsequent phases build on — correct tsconfig, linting, formatting, directory structure, and package.json configured for GitHub distribution.
Output: Compilable TypeScript project with ESLint, Prettier, correct directory layout, config module with defaults and schema validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@code-teacher_spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with package.json, tsconfig, ESLint, Prettier</name>
  <files>package.json, tsconfig.json, .eslintrc.json, .prettierrc, .gitignore</files>
  <action>
    1. Run `npm init -y` to create package.json
    2. Edit package.json:
       - name: "code-teacher"
       - version: "0.1.0"
       - description: "CLI tool that analyzes codebases to surface teachable sections, high-impact sections, and data structure decisions"
       - main: "dist/cli/index.js"
       - bin: { "code-teacher": "dist/cli/index.js" }
       - scripts: { "build": "tsc", "start": "node dist/cli/index.js", "lint": "eslint .", "format": "prettier --write ." }
       - type: "module" (use ESM)
       - files: ["dist/", "agents/definitions/"] (for npm distribution)
       - repository field for GitHub
    3. Install dev dependencies: `npm install -D typescript @types/node eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin prettier`
    4. Create tsconfig.json:
       - target: "ES2022"
       - module: "Node16" / moduleResolution: "Node16"
       - outDir: "dist"
       - rootDir: "."
       - strict: true
       - include: ["cli/**/*.ts", "agents/**/*.ts", "core/**/*.ts", "config/**/*.ts"]
    5. Create .eslintrc.json with @typescript-eslint parser and recommended rules
    6. Create .prettierrc with consistent style (semi: true, singleQuote: true, printWidth: 100)
    7. Create .gitignore with: node_modules/, dist/, .code-teacher-cache/, *.js (in root only), .env
  </action>
  <verify>
    - `npx tsc --noEmit` passes (may need stub files first)
    - `cat package.json | grep '"bin"'` shows code-teacher entry
    - `ls node_modules/.package-lock.json` confirms dependencies installed
  </verify>
  <done>package.json configured for GitHub distribution, TypeScript compiles, ESLint and Prettier configured</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure and stub files with config module</name>
  <files>cli/index.ts, cli/commands/analyze.ts, cli/commands/teachings.ts, cli/commands/sections.ts, cli/output/formatter.ts, cli/output/renderer.ts, agents/definitions/.gitkeep, agents/runner.ts, agents/context.ts, core/file-discovery.ts, core/chunker.ts, core/dependency-graph.ts, core/cache.ts, config/defaults.ts, config/schema.ts</files>
  <action>
    1. Create all directories matching the spec architecture:
       - cli/, cli/commands/, cli/output/
       - agents/, agents/definitions/
       - core/
       - config/
    2. Create cli/index.ts as the entry point with a shebang line (#!/usr/bin/env node) and a basic placeholder that prints version
    3. Create stub files for all modules (empty exports) so TypeScript compiles:
       - cli/commands/analyze.ts, teachings.ts, sections.ts
       - cli/output/formatter.ts, renderer.ts
       - agents/runner.ts, agents/context.ts
       - core/file-discovery.ts, chunker.ts, dependency-graph.ts, cache.ts
    4. Implement config/defaults.ts with the spec's default values:
       - ignore: ["node_modules", "dist", "build", ".git", "__pycache__", "*.min.js", "*.min.css", "*.lock", "package-lock.json", "yarn.lock", "*.png", "*.jpg", "*.gif", "*.svg", "*.ico", "*.woff", "*.woff2", "*.ttf", "*.eot"]
       - maxFileSize: 50000
       - topN: 5
       - provider: undefined (auto-detect)
       - model: undefined (provider default)
       - customAgents: []
    5. Implement config/schema.ts:
       - Define ConfigSchema interface matching code-teacher.config.json format:
         { ignore?: string[], maxFileSize?: number, topN?: number, provider?: string, model?: string, customAgents?: string[] }
       - Export validateConfig(raw: unknown): Config function that validates and merges with defaults
       - Export loadConfig(projectPath: string): Config function that reads code-teacher.config.json if present, validates, and merges with defaults
    6. Verify TypeScript compiles: `npx tsc --noEmit`
  </action>
  <verify>
    - `npx tsc --noEmit` passes with zero errors
    - `ls cli/commands/` shows analyze.ts, teachings.ts, sections.ts
    - `ls config/` shows defaults.ts, schema.ts
    - Config module exports validateConfig and loadConfig
  </verify>
  <done>All spec directories exist, stub files compile, config module validates and loads code-teacher.config.json</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npx eslint .` runs without errors
- [ ] `npx prettier --check .` passes
- [ ] Directory structure matches spec architecture
- [ ] config/defaults.ts exports default values matching spec
- [ ] config/schema.ts validates config files and merges with defaults
- [ ] package.json bin field points to dist/cli/index.js
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TypeScript project compiles cleanly with strict mode
- Config module handles missing, partial, and invalid config files
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-cli/01-01-SUMMARY.md`
</output>
