/**
 * Agent runner
 * Loads agent markdown (.md) files, constructs prompts from their sections,
 * and orchestrates LLM calls for analysis.
 */

export async function runAgent(
  _agentPath: string,
  _context: unknown,
): Promise<Record<string, unknown>> {
  // TODO: Implement in Phase 4
  return {};
}
